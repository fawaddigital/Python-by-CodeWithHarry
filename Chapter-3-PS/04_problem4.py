#4. Replace the double space from problem 3 with single spaces. 

name = "fawad is a good  boy"
print(name.replace("  "," "))
print(name)
    # Strings are immutable which means you cannot change them by executing  functions one them.