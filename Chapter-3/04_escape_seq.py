    # Escape sequence characters** are special codes in Python that start with `\` and are used to represent actions like a new line (`\n`) or a tab space (`\t`).

            # \n for Enter
a = "fawad is a good boy \n but not a bad boy"
print(a)
        # To write text in double quotes inside string
b = "fawad is a good boy\"but not a bad\" boy"
print(b)
                # \t for TAB
c = "fawad \tis \ta \tgood \tboy \tt but not a bad boy"
print(c)

